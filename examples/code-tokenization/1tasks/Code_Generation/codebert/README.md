# CodeBERT Utilities

This folder contains lightweight utilities associated with the CodeBERT-based code generation setup.

The public subset includes:

- BLEU scoring helper.
- Minimal CodeBLEU wrapper.
- Token-mapping utilities for tokenizer perturbation experiments.

It intentionally excludes the full training and evaluation pipeline.
