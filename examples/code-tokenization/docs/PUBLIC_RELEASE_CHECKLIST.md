# Public Release Checklist

- Remove model checkpoints, logs, cached outputs, and generated result files.
- Keep dataset access paths configurable through command-line arguments.
- Avoid embedding workstation-specific absolute paths in metadata files.
- Keep the repository focused on reusable preprocessing and tokenizer-construction code.
- Document clearly which components are intentionally excluded from the public release.
