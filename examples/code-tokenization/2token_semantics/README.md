# Shared-Token Semantics Utilities

This directory contains lightweight public scripts for preparing the masking datasets used in the shared-token analysis.

The public subset focuses on data preparation only and does not include the full downstream model evaluation workflow.
